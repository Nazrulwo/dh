import {
     SAVE_USER
} from './types';

export const saveAction = ( data ) => {
    return dispatch => {
        try {
            // type:SAVE_USER;
            data
        }
        catch (e){}
    }
}